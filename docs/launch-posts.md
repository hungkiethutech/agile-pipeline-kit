# Launch / promotion content (copy-paste ready)

Repo: https://github.com/hungkiethutech/agile-pipeline-kit

---

## Hacker News — "Show HN"
**Title:** Show HN: Agile Pipeline Kit – 8 independent AI agent teams in Claude Code (free)

**Body:**
I built a free, Claude-Code-native Agile pipeline where 8 independent agent "teams" hand off
work by contract instead of one agent doing everything: Requirements (BA), UX/UI Design,
Architecture & DB, Development, black-box QA, Security/Pentest (OWASP Top 10:2025), DevOps,
and Ops. Each team runs as a separate subagent with an isolated context, so you get real
separation of duties — the QA team can't read the dev's source and tests the running app
like a real user; the Dev team builds strictly to the architect's schema + OpenAPI contract.
It runs entirely on a Claude subscription (no paid API), scaffolds a new project with one
command, and each stage's "engine" is pluggable (a skill or a popular GitHub repo). Feedback
welcome.

---

## Reddit (r/ClaudeAI, r/AI_Agents, r/opensource)
**Title:** I made a free 8-team AI software pipeline that runs inside Claude Code

**Body:**
Instead of one "do-everything" agent, this splits delivery into 8 independent teams that
hand off by contract (with a Definition of Done gate between each): BA → Design ∥
Architecture → Dev → black-box QA → Security/Pentest (OWASP Top 10:2025) → DevOps → Ops.
Independent QA tests the running app as a real user (no peeking at source); Dev builds to the
architect's schema + API contract. Free (subscription only), one-command setup, pluggable
engines per stage (plug in spec-kit, a design taste skill, Playwright, upptime, etc.).
MIT-licensed. Would love feedback and contributors: <repo link>

---

## X / Twitter (thread)
1/ I built an open-source Agile pipeline for Claude Code: 8 independent AI agent "teams"
that hand off by contract — not one agent doing everything. Free, MIT. 🧵
2/ Teams: Requirements → Design ∥ Architecture → Dev → black-box QA → Security/Pentest
(OWASP Top 10:2025) → DevOps → Ops. Each is a separate subagent w/ isolated context.
3/ Separation of duties: QA tests the running app like a real user and can't read the dev's
source; Dev builds strictly to the schema + OpenAPI contract from Architecture.
4/ Runs on your Claude subscription (no paid API). One-command scaffold. Each stage's engine
is pluggable (a skill or a popular GitHub repo). ⭐ <repo link>

---

## LinkedIn
Open-sourced "Agile Pipeline Kit" — a free way to run software delivery in Claude Code as 8
independent agent teams (BA, Design, Architecture/DB, Dev, black-box QA, Security/Pentest,
DevOps, Ops) that hand off by contract, with a Definition-of-Done gate between stages. It
brings separation of duties to AI coding: an independent QA team that tests like a real user,
and a dedicated OWASP Top 10:2025 security gate. MIT-licensed, no paid API. Link in comments.

---

## dev.to / Hashnode — blog outline
Title: "How I built an 8-team AI dev pipeline that runs free in Claude Code"
- The problem: one agent doing everything → no separation of duties, weak QA/security.
- The idea: independent teams + contract handoff + Definition of Done.
- Deep dive: black-box QA, Dev-to-contract, the OWASP Top 10:2025 security gate.
- Pluggable engines per stage (skills + GitHub repos).
- Demo: one ticket end-to-end (link to examples/).
- Try it: one-command setup.

---

## Awesome-list entry (open a PR to these lists)
Target lists: `awesome-claude-code`, `awesome-ai-agents`, `awesome-agents`.

Entry line:
`- [Agile Pipeline Kit](https://github.com/hungkiethutech/agile-pipeline-kit) — Free
Claude-Code-native Agile pipeline: 8 independent agent teams (BA, Design, Architecture/DB,
Dev, black-box QA, Security/Pentest, DevOps, Ops) with contract-based handoff and per-stage
Definition of Done.`
